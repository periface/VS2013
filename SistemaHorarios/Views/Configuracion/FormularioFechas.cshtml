@model SERVICIOS.Models.MFechaEspecial
@{
    ViewBag.Title = "FormularioFechas";
    Layout = null;
}

    <div class="row">
        <div class="col-lg-12">
            <div class="form-group">
                <label class="control-label">Seleccione el tipo de fecha</label>
                <select name="tipo" class="form-control">
                    <option value="1">
                        Dia(s) Festivo(s)
                    </option>
                    <option value="2">
                        Forzar dia(s) Laboral(es)
                    </option>
                    <option value="3">
                        Dia(s) Restrictivo(s)
                    </option>
                </select>
            </div>
            <div class="form-group">
                @Html.HiddenFor(a => a.fin)
                @Html.HiddenFor(a => a.inicio)
                @Html.LabelFor(a => a.titulo, new { @class = "control-label" })
                @Html.TextAreaFor(a => a.titulo, new { @class = "form-control" })
            </div>
            <div class="form-group">
                @Html.LabelFor(a => a.descripcion, new { @class = "control-label" })
                @Html.TextAreaFor(a => a.descripcion, new { @class = "form-control" })
            </div>
            <div class="checkbox">
                <label>
                    @Html.CheckBoxFor(a => a.todoElDia) Esta restricción o fecha aplica todo el dia
                </label>
            </div>
                
        </div>
    </div>

    